services:
  - type: web
    name: pancreatic-cancer-segmentation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn deployment.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
